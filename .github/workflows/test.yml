name: Deploy to Amazon ECS

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
    - run: if [ $var -e 12 ]; then echo "equal"; else echo "not equal"; fi
      env:
        var: ${{ TEST_VARIABLE }}
      
